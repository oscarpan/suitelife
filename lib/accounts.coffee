# Accounts.onCreateUser (options, user) ->
#   #pass the suite in the options http://docs.meteor.com/#/full/accounts_oncreateuser
#   user.profile['suite_id'] = options.suite_id
#   user

# Accounts.validateNewUser (user) ->
#   IsNumeric(user.profile['suite_id'])
